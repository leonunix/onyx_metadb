# onyx-metadb Agent Instructions

`CLAUDE.md` is the canonical agent guide for this crate. Read it before
changing code, tests, soak harness behavior, or public documentation.

Do not duplicate project rules here; update `CLAUDE.md` instead so all agents
share one source of truth.
